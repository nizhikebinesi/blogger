<h1>Articles Tagged with <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |article| %>
    <li><h4><%= link_to article.title, article_path(article) %></h4></li>

  <% end %>
</ul>
<%= link_to "delete", tag_path(@tag), method: :delete, data: {confirm: "Really delete this tag?"} %>
